*First Install the CrystalReportsSetup.msi
*open the database.sql file and execute it on your MSSQL server
*change the user ID and the password for the sql server in the web.config file in the solution to which matches the login details of your sql server (If you haven't setup a username or password just take off the user ID and password property from the connection string)


*default(admin) user login is
	username - admin@a.com
	pasword  - 123
*run and enjoy the system